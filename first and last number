#include<bits/stdc++.h>
using namespace std;
int main(){
    int a;
    cin >> a;
    int arr[a];
    for(int i =0;i<a;i++){
        cin>> arr[i];
    }
    int k ;
    cin >> k;
    int first =-1;
    int last;
    for(int i =0;i<a;i++){
        if(arr[i] == k && first == -1){
            first =i;
        }
        if(first != -1 && arr[i] == k){
            last =i;
        }
    }
    cout <<first<<" " << last ;
    return 0;
}
