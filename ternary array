#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin >> n;
int ans =0;
    for (int i=0;i<n;i++){
int x;
cin >> x;
long long best = LLONG_MAX;
for(int j=0;j<=2;j++){
    if(x != j){
        best = min(best , llabs(x-j));
    }
}
ans += best;
    }
    cout << ans <<endl;
}
