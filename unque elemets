#include<bits/stdc++.h>
using namespace std;
int main(){
    int a;
    cin >> a;
    vector<int> arr(a);
    for(int i=0;i<a;i++){
        cin >> arr[i];
    }

unordered_map<int ,int> freq;
    for(int x : arr){
        freq[x]++;
    }
    unordered_set<int> printed;
    for(int x : arr){
        if(freq[x]==1 && printed.find(x)==printed.end()){
            cout << x << " ";
            printed.insert(x);
        }
    }
    return 0;
    }
