// User function Template for C++

string toBinary(int n) {
    // Your code here
    string binary ="";
    while(n >0){
        binary += (n%2)+'0';
        n = n/2;
    }
    reverse(binary.begin(),binary.end());
    return binary;
}
