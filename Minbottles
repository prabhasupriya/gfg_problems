include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int n;
	cin >> n;
	while(n-- >0){
	    int a,b;
	    cin >>a>>b;
	    int s=0;
	    vector<int> ans(a);
	    for(int i=0;i<a;i++){
	        cin >>ans[i];
	        s =s+ans[i];
	    }
	    cout << ceil((double)s/(double)b)<<endl;
	}
	    
}
