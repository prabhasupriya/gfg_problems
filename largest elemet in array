class Solution {
  public:
    int largest(vector<int> &arr) {
        // code here
        int mainmax;
        int maxq = arr[0];
        for (int i=1;i<arr.size();i++){
            maxq = max(maxq , arr[i]);
            
        }
        return maxq;
    }
};
