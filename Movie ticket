#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
int a;
cin >> a;
while(a-- >0){
    int n,m,b,c,d;
    cin >> n>> m >> b>> c>>d;
    int l = min(n,m);
    int ans = l*d;
    if(n-m  <0){
        ans = ans + (m-n)*c;
    }
    else{
        ans = ans +(n-m)*b;
    }
    cout << ans << endl;
}
}

