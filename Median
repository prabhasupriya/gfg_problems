int median(vector<int>& arr) {
    int ans = 0;
    int n = arr.size();

    sort(arr.begin(), arr.end());   // FIXED

    if(n % 2 == 0){
        ans = (arr[n/2] + arr[(n-1)/2]) / 2;
    }
    else{
        ans = arr[n/2];
    }
    return ans;
}
