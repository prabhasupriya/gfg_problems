void sortArray(int a[], char b[], int n) {

    // code here
    pair<int,int> res[n];
    for(int i=0;i<n;i++){
        res[i].first = a[i];
        res[i].second = b[i];
    }
    sort(res, res+n);
    for(int i=0;i<n;i++){
        b[i] = res[i].second;
    }
}
