class Solution {
  public:

    int subsequenceSum(string s) {
        // Your code goes here
        int length = s.size();
        int p = pow(2,length-1);
        int sum =0;
        for(int i=0;i<length;i++){
            sum += p*(s[i]-'0');
        }
        return sum ;
    }
};
