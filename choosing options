#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
int n;
cin >> n;
while(n-- >0){
    int k;
    cin >> k;
    int a,b,c,d;
    cin >>a>>b>>c>>d;
    int m = max(a, max(b,c));
    int l = max(m , d);
    cout << l << endl;
}
}
