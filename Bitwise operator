 class Solution {
public:
    int xorOperation(int n, int start) {
        vector<int > ans;
        int bit=0;
        for(int i=0;i<n;i++){
            ans.push_back((start +(2*i)));
            bit = bit^(start+(2*i));
        }
        return bit;
    }
};       
